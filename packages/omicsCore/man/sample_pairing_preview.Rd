% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration-sample-link.R
\name{sample_pairing_preview}
\alias{sample_pairing_preview}
\title{The pairing two layers would be integrated on}
\usage{
sample_pairing_preview(project, tag_a, tag_b)
}
\arguments{
\item{project}{An \link{omics_project}.}

\item{tag_a, tag_b}{Experiment tags.}
}
\value{
A list with \code{pairs} (data frame, possibly zero rows) and
\code{source}, one of \code{"linked"} (a pairing saved on the project),
\code{"donor"} (a donor column in both layers), \code{"sample_id"} (ids that
match outright), \code{"suggested"} (guessed from the ids) or \code{"none"}.
}
\description{
What the Integration view shows before it runs anything. One row per
donor, one column per layer, and a \code{source} saying where the pairing
came from -- because "these two samples are the same person" is an
assertion the reader should be able to check rather than infer.
}
\seealso{
Other integrate: 
\code{\link{derive_sample_link}()},
\code{\link{suggest_sample_link}()}
}
\concept{integrate}
