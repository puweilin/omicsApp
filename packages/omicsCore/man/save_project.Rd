% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/persistence.R
\name{save_project}
\alias{save_project}
\title{Save an omics_project to disk}
\usage{
save_project(project, path, overwrite = FALSE)
}
\arguments{
\item{project}{An \code{\link[=is_omics_project]{omics_project}}.}

\item{path}{Output path. The file extension is up to the caller, but
the convention is \code{.omp} for projects produced by \code{omicsCore}.}

\item{overwrite}{If \code{FALSE} (default) and \code{path} already exists,
\code{save_project()} raises an error. Set to \code{TRUE} to replace.}
}
\value{
Invisibly returns \code{path}.
}
\description{
Writes the full project (every experiment, sample_link, and any
attached \code{analysis_bundle}s) to a single \code{.omp} file using \code{qs2}. The
write is atomic: the payload first goes to \code{path.tmp}, then renames
over \code{path}, so an interrupted save will not corrupt an existing file.
}
\details{
Requires the \code{qs2} package; install it with \code{install_optional("persistence")}.
}
\examples{
\dontrun{
  p <- omics_project("demo", experiments = list(proteo = my_input))
  save_project(p, "demo.omp")
  q <- load_project("demo.omp")
}
}
\seealso{
Other persistence: 
\code{\link{export_bundle}()},
\code{\link{export_report}()},
\code{\link{load_project}()}
}
\concept{persistence}
