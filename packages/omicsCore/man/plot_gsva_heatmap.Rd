% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-gsva.R
\name{plot_gsva_heatmap}
\alias{plot_gsva_heatmap}
\title{GSVA heatmap}
\usage{
plot_gsva_heatmap(
  bundle,
  top_n = 25L,
  pathways = NULL,
  meta_df = NULL,
  annotation_cols = NULL,
  scale = c("row", "none", "column"),
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  title = "GSVA"
)
}
\arguments{
\item{bundle}{An \code{\link[=is_analysis_bundle]{analysis_bundle}} produced by
\code{\link[=run_gsva]{run_gsva()}}.}

\item{top_n}{Number of pathways to plot when \code{pathways} is \code{NULL}.
Selected by variance across samples.}

\item{pathways}{Optional character vector of pathway names to plot.}

\item{meta_df}{Optional sample metadata \code{data.frame} (or named list of
columns) used for column annotations. Rows must be indexable by sample
id (column names of the score matrix).}

\item{annotation_cols}{Optional character vector of columns from
\code{meta_df} to surface as a \code{HeatmapAnnotation}.}

\item{scale}{One of \code{"row"} (default), \code{"none"}, or \code{"column"}.}

\item{cluster_rows, cluster_cols}{Whether to cluster rows / columns.}

\item{title}{Heatmap title.}
}
\value{
A \code{ComplexHeatmap::Heatmap} object when \code{ComplexHeatmap} is
available, otherwise a \code{ggplot} tile plot.
}
\description{
Draws a pathways-by-samples heatmap from a \code{\link[=run_gsva]{run_gsva()}} bundle. When
\code{ComplexHeatmap} is installed the function returns a \code{Heatmap} object
(recommended); otherwise it falls back to a ggplot2 tile plot so the
function is always usable from a clean install.
}
\seealso{
Other enrich: 
\code{\link{filter_enrich_results}()},
\code{\link{list_gene_sets}()},
\code{\link{plot_enrichment}()},
\code{\link{plot_gsea}()},
\code{\link{run_enrichment}()},
\code{\link{run_gsva}()}
}
\concept{enrich}
