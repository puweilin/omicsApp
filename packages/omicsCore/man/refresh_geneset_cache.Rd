% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geneset-refresh.R
\name{refresh_geneset_cache}
\alias{refresh_geneset_cache}
\title{Refresh the on-disk gene-set cache}
\usage{
refresh_geneset_cache(
  databases = SUPPORTED_ENRICH_DATABASES,
  organism = "Hs",
  max_age_days = 30,
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{databases}{Database keys to refresh. Defaults to all supported
databases; only \code{"kegg"} involves the network.}

\item{organism}{Organism shorthand (e.g. \code{"Hs"}, \code{"Mm"}).}

\item{max_age_days}{Skip databases whose cache file is younger than this.}

\item{force}{If \code{TRUE}, refresh regardless of age.}

\item{quiet}{If \code{TRUE}, suppress progress messages.}
}
\value{
Invisibly, a \code{data.frame} with one row per database: \code{database},
\code{action} (\code{"refreshed"}, \code{"fresh"}, or \code{"failed"}), \code{n_sets}, \code{path}.
}
\description{
Rebuilds the qs2 gene-set cache that \code{\link[=run_enrichment]{run_enrichment()}}, \code{\link[=run_gsva]{run_gsva()}},
and \code{\link[=list_gene_sets]{list_gene_sets()}} read. For \code{"kegg"} the table is fetched live from
the KEGG REST API (current pathway definitions, in symbol space -- about
twice the pathways of the bundled 2011 \code{KEGG_LEGACY} snapshot); all other
databases are re-snapshotted from the installed \code{msigdbr}. Every table is
stamped with a \code{gs_source} so downstream results can name the pathway
definitions they used.
}
\details{
The cache directory is \code{OMICSCORE_GENESET_CACHE} when set, otherwise a
per-user directory (\code{tools::R_user_dir("omicsCore", "cache")}). Once a
live KEGG table is in place it is kept current automatically: an
enrichment run that finds it older than \code{OMICSCORE_GENESET_TTL_DAYS}
(default 30) re-fetches it first, falling back to the stale table if the
network is unavailable. Set \code{OMICSCORE_GENESET_TTL_DAYS=0} to disable.

KEGG-derived caches are for local use only; KEGG's license does not
permit redistributing them.
}
\examples{
\dontrun{
  refresh_geneset_cache("kegg")
  geneset_cache_status()
}
}
\seealso{
Other enrich: 
\code{\link{filter_enrich_results}()},
\code{\link{geneset_cache_status}()},
\code{\link{list_gene_sets}()},
\code{\link{plot_enrichment}()},
\code{\link{plot_gsea}()},
\code{\link{plot_gsva_heatmap}()},
\code{\link{run_enrichment}()},
\code{\link{run_gsva}()}
}
\concept{enrich}
