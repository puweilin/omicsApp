% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-gsva.R
\name{run_gsva}
\alias{run_gsva}
\title{Run Gene Set Variation Analysis}
\usage{
run_gsva(
  input,
  gene_sets = NULL,
  database = "hallmark",
  organism = "Hs",
  method = c("gsva", "ssgsea"),
  min_size = 10L,
  max_size = 500L,
  kcdf = NULL,
  ...
)
}
\arguments{
\item{input}{A validated \code{omics_input}.}

\item{gene_sets}{Optional named list of character vectors. When supplied,
\code{database} is ignored.}

\item{database}{Database key when \code{gene_sets} is \code{NULL}. See
\code{\link[=list_gene_sets]{list_gene_sets()}} for supported keys.}

\item{organism}{Organism shorthand (e.g. \code{"Hs"}).}

\item{method}{One of \code{"gsva"} (default) or \code{"ssgsea"}.}

\item{min_size, max_size}{Min / max gene-set size after intersecting with
the assay's feature set.}

\item{kcdf}{Kernel CDF used by \code{GSVA::gsvaParam()} — typically
\code{"Gaussian"} for log-scale data, \code{"Poisson"} for raw counts.}

\item{...}{Reserved for future arguments.}
}
\value{
An \code{\link[=is_analysis_bundle]{analysis_bundle}} with
\code{results$gsva_matrix} (pathways × samples) and
\code{results$gsva_gene_sets} (the gene-set list actually used).
}
\description{
Computes sample-level pathway activity scores via the \code{GSVA} package. The
caller may either pass a named list of gene sets directly (\code{gene_sets})
or pick one of the supported MSigDB databases (\code{database}). RNA-seq raw
counts are log2-transformed before scoring.
}
\examples{
\dontrun{
  gsva_bundle <- run_gsva(input, database = "hallmark")
  head(gsva_bundle$results$gsva_matrix[, 1:4])
}
}
\seealso{
Other enrich: 
\code{\link{filter_enrich_results}()},
\code{\link{geneset_cache_status}()},
\code{\link{list_gene_sets}()},
\code{\link{plot_enrichment}()},
\code{\link{plot_gsea}()},
\code{\link{plot_gsva_heatmap}()},
\code{\link{refresh_geneset_cache}()},
\code{\link{run_enrichment}()}
}
\concept{enrich}
