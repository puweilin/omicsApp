% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-enrichment.R
\name{run_enrichment}
\alias{run_enrichment}
\title{Run pathway enrichment from a differential bundle}
\usage{
run_enrichment(
  diff_bundle,
  type = c("ora", "gsea"),
  database = c("hallmark", "kegg", "reactome", "go_bp", "go_mf", "go_cc", "wikipathways"),
  organism = "Hs",
  direction = c("both", "up", "down"),
  p_cutoff = 0.05,
  p_preference = c("adjusted", "raw"),
  effect_cutoff = NULL,
  p_adjust_method = "BH",
  min_size = 10L,
  max_size = 500L,
  ...
)
}
\arguments{
\item{diff_bundle}{An \code{\link[=is_analysis_bundle]{analysis_bundle}} produced
by \code{\link[=run_diff]{run_diff()}}.}

\item{type}{One of \code{"ora"} or \code{"gsea"}.}

\item{database}{One of \code{"hallmark"}, \code{"kegg"}, \code{"reactome"},
\code{"wikipathways"}, \code{"go_bp"}, \code{"go_mf"}, \code{"go_cc"}. Pass a character
vector to query multiple databases.}

\item{organism}{Organism shorthand (e.g. \code{"Hs"}).}

\item{direction}{One of \code{"both"} (default), \code{"up"}, or \code{"down"}.}

\item{p_cutoff}{Significance cutoff used both to select diff features
(ORA) and to bound the enrichment result table.}

\item{p_preference}{For ORA feature selection: \code{"adjusted"} (default) or
\code{"raw"}.}

\item{effect_cutoff}{Optional |effect| cutoff for ORA feature selection.}

\item{p_adjust_method}{Multiple-testing correction method.}

\item{min_size, max_size}{GSEA min/max gene-set sizes.}

\item{...}{Reserved for backend-specific extensions.}
}
\value{
An \code{\link[=is_analysis_bundle]{analysis_bundle}} with
\code{results$enrich_result_df} (standardized schema) and
\code{results$enrich_object} (named list of clusterProfiler objects keyed by
database, or for ORA by \verb{<direction>__<database>}).
}
\description{
Single entry point for over-representation analysis (\code{type = "ora"}) and
rank-based gene-set enrichment analysis (\code{type = "gsea"}). Both backends
run against the requested MSigDB database in symbol space via
\code{clusterProfiler::enricher()} / \code{GSEA()}, so no \verb{org.*} annotation
package is required.
}
\details{
For ORA, features are split into up/down sets unless
\code{direction = "both"}; for GSEA, the full ranked vector is always passed
to the backend and \code{direction} filters the standardized output.

For GSVA-style sample-level scoring, see \code{\link[=run_gsva]{run_gsva()}}.
}
\examples{
\dontrun{
  diff <- run_diff(input, method = "limma", analysis_type = "group",
                   group_col = "treatment",
                   control_group = "DMSO", case_group = "Drug")
  enr <- run_enrichment(diff, type = "ora", database = "hallmark")
  head(enr$results$enrich_result_df)
}
}
\seealso{
Other enrich: 
\code{\link{filter_enrich_results}()},
\code{\link{geneset_cache_status}()},
\code{\link{list_gene_sets}()},
\code{\link{plot_enrichment}()},
\code{\link{plot_gsea}()},
\code{\link{plot_gsva_heatmap}()},
\code{\link{refresh_geneset_cache}()},
\code{\link{run_gsva}()}
}
\concept{enrich}
