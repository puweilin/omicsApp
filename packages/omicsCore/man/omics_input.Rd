% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-omics_input.R
\name{omics_input}
\alias{omics_input}
\title{Single-omics input container}
\usage{
omics_input(
  expr_mat,
  meta_df,
  feature_df,
  omics_type,
  assay_type = NULL,
  raw_mat = NULL,
  normalized_mat = NULL,
  raw_object = NULL,
  source_fingerprint = NULL,
  source_path = NULL
)
}
\arguments{
\item{expr_mat}{Main expression matrix; rows are features, columns are
samples. A \code{data.frame} is coerced to a matrix.}

\item{meta_df}{Sample metadata; \code{rownames(meta_df)} must include
\code{colnames(expr_mat)}.}

\item{feature_df}{Feature metadata containing a \code{feature_id} column.}

\item{omics_type}{Omics modality, one of \link{SUPPORTED_OMICS_TYPES}.}

\item{assay_type}{Assay semantic label, ideally one of the values listed for
this \code{omics_type} in \link{SUPPORTED_ASSAY_TYPES} (e.g. \code{"raw_intensity"},
\code{"normalized_intensity"}, \code{"raw_count"}). This is the only record of what
scale \code{expr_mat} is on -- nothing downstream re-derives it -- so an
inaccurate label silently changes what the analysis backends do. Superseded
spellings in \link{DEPRECATED_ASSAY_TYPE_ALIASES} are rewritten with a warning.}

\item{raw_mat}{Optional raw matrix carried alongside \code{expr_mat}.}

\item{normalized_mat}{Optional normalized matrix carried alongside \code{expr_mat}.}

\item{raw_object}{Optional upstream raw object (e.g. a \code{DESeqDataSet} or
\code{SummarizedExperiment}) preserved for reference.}

\item{source_fingerprint}{Optional string identifying the file this
input was parsed from. Callers that re-import into a live project use
it to tell "the user picked the same file again" from "the data
changed"; analyses computed on the previous data are only valid for
the former. \code{NULL} for inputs built directly from matrices.}

\item{source_path}{Optional path to the archived copy of that file.
\code{\link[=export_script]{export_script()}} points its \code{read_omics()} line at it, which is
what makes an exported script runnable rather than illustrative.}
}
\value{
An object of class \code{omics_input}.
}
\description{
Constructs the canonical input object consumed by all \code{omicsCore} analysis
functions. The argument order is \code{expr_mat}, \code{meta_df}, \code{feature_df},
\code{omics_type}, \code{assay_type} to match the contract in
\code{docs/export-manifest.md}.
}
\details{
\code{expr_mat} must be a numeric matrix with sample column names and feature
row names. \code{meta_df} must have rownames matching the sample column names of
\code{expr_mat}. \code{feature_df} must contain a \code{feature_id} column.
}
\examples{
expr <- matrix(rnorm(20), nrow = 4, dimnames = list(
  paste0("g", 1:4), paste0("s", 1:5)
))
meta <- data.frame(group = c("A", "A", "B", "B", "B"),
                   row.names = paste0("s", 1:5))
feat <- data.frame(feature_id = paste0("g", 1:4),
                   row.names = paste0("g", 1:4))
omics_input(expr, meta, feat, omics_type = "proteomics",
            assay_type = "normalized_intensity")
}
\seealso{
Other omics_input: 
\code{\link{SUPPORTED_ASSAY_TYPES}},
\code{\link{check_assay_scale}()},
\code{\link{drop_meta_na}()},
\code{\link{is_omics_input}()},
\code{\link{normalize_omics}()},
\code{\link{select_complete_cases}()},
\code{\link{subset_omics}()},
\code{\link{subset_omics_features}()},
\code{\link{subset_omics_samples}()},
\code{\link{summarize_omics}()},
\code{\link{validate_omics_input}()}
}
\concept{omics_input}
