% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-heatmap.R
\name{plot_heatmap}
\alias{plot_heatmap}
\title{Top-features expression heatmap}
\usage{
plot_heatmap(
  x,
  input = NULL,
  n_top = 50L,
  features = NULL,
  scale = c("row", "none", "column"),
  annotation_cols = NULL,
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  show_rownames = NULL,
  title = NULL
)
}
\arguments{
\item{x}{Either an \code{\link[=omics_input]{omics_input}} or an
\code{\link[=is_analysis_bundle]{analysis_bundle}} from \code{\link[=run_diff]{run_diff()}}.}

\item{input}{Required when \code{x} is an analysis_bundle: the
\code{omics_input} whose \code{expr_mat} should be used for the tiles.}

\item{n_top}{Number of features to display.}

\item{features}{Optional character vector of \code{feature_id}s to force.
Overrides \code{n_top}.}

\item{scale}{One of \code{"row"} (default), \code{"none"}, or \code{"column"}.}

\item{annotation_cols}{Optional character vector of columns from
\code{input$meta_df} to surface as a column annotation.}

\item{cluster_rows, cluster_cols}{Whether to cluster rows / columns.}

\item{show_rownames}{If \code{NULL}, auto-decide based on row count.}

\item{title}{Plot title.}
}
\value{
A \code{ComplexHeatmap::Heatmap} object when \code{ComplexHeatmap} is
available, otherwise a \code{ggplot} tile plot.
}
\description{
Two dispatch modes:
}
\details{
\itemize{
\item \strong{\code{omics_input} mode} -- pass an \code{omics_input} as the first argument.
The function selects the top \code{n_top} features by row variance (after
\code{coerce_to_continuous()}) and draws a samples-by-features heatmap.
\item \strong{\code{analysis_bundle} mode} -- pass a \code{\link[=run_diff]{run_diff()}} bundle and the
corresponding \code{omics_input} via the \code{input} argument. Features are
selected by ascending adjusted p-value.
}

When \code{ComplexHeatmap} is installed the function returns a
\code{ComplexHeatmap::Heatmap}; otherwise it falls back to a ggplot2 tile
plot so the function is always usable from a clean install.
}
\examples{
\dontrun{
  # From an omics_input
  plot_heatmap(input, n_top = 30)

  # From a diff bundle
  b <- run_diff(input, ...)
  plot_heatmap(b, input = input, n_top = 50)
}
}
\seealso{
Other diff: 
\code{\link{filter_diff_results}()},
\code{\link{make_ranked_features}()},
\code{\link{plot_feature_expression}()},
\code{\link{plot_ma}()},
\code{\link{plot_pca}()},
\code{\link{plot_volcano}()},
\code{\link{run_diff}()},
\code{\link{run_diff_continuous}()}
}
\concept{diff}
