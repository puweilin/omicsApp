% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-omics_project.R
\name{omics_project}
\alias{omics_project}
\title{Multi-omics project container}
\usage{
omics_project(
  name,
  experiments = list(),
  sample_link = NULL,
  feature_link = NULL,
  metadata = list()
)
}
\arguments{
\item{name}{Human-readable project label.}

\item{experiments}{Named list of \code{\link[=omics_input]{omics_input()}} objects. Names become
experiment tags. May be empty; use \code{\link[=add_experiment]{add_experiment()}} to populate.}

\item{sample_link}{Optional \code{data.frame} with at least the columns
\code{tag}, \code{sample_id}, \code{donor_id}. \code{tag} matches a name in \code{experiments}.
Each row links a per-experiment sample to a shared donor.}

\item{feature_link}{Optional \code{data.frame} mapping feature IDs across
omics (e.g. columns \code{uniprot}, \code{gene_symbol}).}

\item{metadata}{Optional named list of arbitrary project-level metadata.}
}
\value{
An object of class \code{omics_project}.
}
\description{
An \code{omics_project} groups multiple \code{\link[=omics_input]{omics_input()}} experiments together
with optional cross-omics mapping tables, so that integration analyses
(RNA × Protein, ActivePathways, etc.) can operate on a single root object.
}
\details{
Each entry in \code{experiments} is one omics layer (e.g. \code{"proteomics"},
\code{"rnaseq"}), indexed by an arbitrary tag. \code{sample_link} maps sample IDs
across layers so paired samples can be aligned; \code{feature_link} maps
feature IDs (e.g. UniProt accession ↔ gene symbol) so cross-omics
integration can join on shared identifiers.
}
\examples{
p <- omics_project(name = "demo")
p
}
\seealso{
Other omics_project: 
\code{\link{add_experiment}()},
\code{\link{experiment_tags}()},
\code{\link{is_omics_project}()},
\code{\link{remove_experiment}()}
}
\concept{omics_project}
