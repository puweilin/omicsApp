% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-script.R
\name{export_script}
\alias{export_script}
\title{Export a project's analysis history as a runnable R script}
\usage{
export_script(project, path = NULL, include_plots = TRUE)
}
\arguments{
\item{project}{An \code{\link[=is_omics_project]{omics_project}}.}

\item{path}{Optional file to write to. The lines are returned either
way.}

\item{include_plots}{Whether to append the plotting calls. On by
default: the Shiny views draw through these same functions, so the
figures the script produces are the figures the user was looking
at. Set \code{FALSE} for a script that only recomputes the numbers.}
}
\value{
Character vector of script lines, invisibly when \code{path} is
given.
}
\description{
Renders the calls that produced a project's analyses, in the order
they depend on each other, as an R script that reproduces them.
Arguments appear as they were \emph{resolved}: an analysis run with
\code{method = "auto"} is emitted with the engine that auto selected, so
the script and the report agree.
}
\details{
Whatever cannot be reconstructed faithfully is flagged with a \code{NOTE}
comment in the script rather than approximated — a script that reads
correctly but computes something else is worse than none.

The input line points at the archived upload when the project records
one (see \code{source_path} on \code{\link[=omics_input]{omics_input()}}); otherwise it emits a
placeholder path for the reader to fill in.
}
\examples{
\dontrun{
  export_script(project, "reproduce.R")
}
}
\seealso{
Other persistence: 
\code{\link{export_bundle}()},
\code{\link{export_report}()},
\code{\link{load_project}()},
\code{\link{save_project}()}
}
\concept{persistence}
