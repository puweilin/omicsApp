% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-bundle.R
\name{export_bundle}
\alias{export_bundle}
\title{Export an analysis_bundle to disk}
\usage{
export_bundle(
  bundle,
  dir,
  formats = c("xlsx", "tsv", "pdf"),
  prefix = NULL,
  plots = NULL,
  width = 7,
  height = 5
)
}
\arguments{
\item{bundle}{An \code{\link[=is_analysis_bundle]{analysis_bundle}}.}

\item{dir}{Output directory. Created if it does not exist.}

\item{formats}{Character vector of table/plot formats. Tables support
\code{"xlsx"} and \code{"tsv"}; plots support \code{"pdf"} and \code{"png"}. Defaults to
\code{c("xlsx", "tsv", "pdf")}.}

\item{prefix}{Optional file-name prefix. Useful when exporting
multiple bundles to the same directory.}

\item{plots}{Optional named list of plot objects to write alongside the
tables (e.g. \code{list(volcano = plot_volcano(bundle))}). Both \code{ggplot}
and \code{ComplexHeatmap} objects are supported.}

\item{width, height}{Plot dimensions in inches. Defaults \verb{7 x 5}.}
}
\value{
A \code{data.frame} artifact registry (one row per written file).
}
\description{
Writes the data-bearing fields of a bundle (\verb{results$*_df} /
\verb{results$*_matrix}), provenance (\code{params}), and -- if \code{plots} is
non-empty -- the supplied ggplot/heatmap objects, in a target
directory.
}
\details{
Output layout under \code{dir}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{<dir>/
  <prefix><bundle_name>_<table>.xlsx
  <prefix><bundle_name>_<table>.tsv
  <prefix><bundle_name>_<matrix>.tsv
  <prefix><bundle_name>_<plot>.pdf      (or .png)
  <prefix><bundle_name>_params.json
}\if{html}{\out{</div>}}

\code{prefix} defaults to \code{""} (no prefix). The bundle's existing artifact
registry is merged with the rows added by this call so downstream
code can keep tracking every file from one entry point.
}
\examples{
\dontrun{
  d <- run_diff(input, method = "ttest", analysis_type = "group",
                group_col = "g", control_group = "ctrl", case_group = "case")
  export_bundle(d, dir = "out/", plots = list(volcano = plot_volcano(d)))
}
}
\seealso{
Other persistence: 
\code{\link{export_report}()},
\code{\link{export_script}()},
\code{\link{load_project}()},
\code{\link{save_project}()}
}
\concept{persistence}
