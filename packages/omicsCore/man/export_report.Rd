% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-report.R
\name{export_report}
\alias{export_report}
\title{Render an HTML report for an omics_project}
\usage{
export_report(
  project,
  path,
  format = c("html", "pdf"),
  template = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{project}{An \code{\link[=is_omics_project]{omics_project}}.}

\item{path}{Output path (file extension is inferred from \code{format}).}

\item{format}{\code{"html"} (default) or \code{"pdf"}. PDF requires a working
LaTeX engine in the user environment.}

\item{template}{Optional path to a custom Rmd template. Defaults to
the package's \code{inst/rmd/default-report.Rmd}.}

\item{overwrite}{If \code{FALSE} (default) and \code{path} exists, raise an
error.}
}
\value{
Invisibly returns \code{path}.
}
\description{
Renders a self-contained HTML (or PDF) report summarising every
experiment and any \code{analysis_bundle} objects attached to a project.
The bundled \code{inst/rmd/default-report.Rmd} template is used unless a
custom path is supplied.
}
\details{
Bundles can be attached to the project in a \code{bundles} slot
(e.g. \code{project$bundles$diff <- run_diff(...)}); the template walks
over \code{names(project$bundles)} and emits per-bundle parameter and
top-rows tables.

Requires the \code{rmarkdown} package.
}
\examples{
\dontrun{
  p <- omics_project("demo", experiments = list(proteo = my_input))
  p$bundles <- list(diff = run_diff(my_input, ...))
  export_report(p, "demo.html")
}
}
\seealso{
Other persistence: 
\code{\link{export_bundle}()},
\code{\link{load_project}()},
\code{\link{save_project}()}
}
\concept{persistence}
