% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install-optional.R
\name{install_optional}
\alias{install_optional}
\title{Install optional dependency groups}
\usage{
install_optional(
  group = c("rnaseq", "proteomics", "enrichment", "imputation", "viz", "persistence",
    "all"),
  ask = interactive(),
  upgrade = FALSE
)
}
\arguments{
\item{group}{One of \code{"rnaseq"}, \code{"proteomics"}, \code{"enrichment"},
\code{"imputation"}, \code{"viz"}, \code{"persistence"}, or \code{"all"}.}

\item{ask}{If \code{TRUE} (default in interactive sessions), prompt before
installing.}

\item{upgrade}{If \code{TRUE}, allow upgrading already-installed packages.
Defaults to \code{FALSE}.}
}
\value{
Invisibly returns a character vector of the packages that were
targeted for installation.
}
\description{
Installs the on-demand \code{Suggests} packages required by a given backend
group. Uses \code{pak::pkg_install()} if available, otherwise falls back to
\code{BiocManager::install()} (for Bioconductor packages) and
\code{utils::install.packages()} (for CRAN packages).
}
\details{
This function exists so that a fresh \code{install.packages("omicsCore")} can
stay small on restricted environments where Docker / system installs are
not available. Users opt in to heavy backends only when they need them.
}
\examples{
\dontrun{
  install_optional("rnaseq")
  install_optional("all", ask = FALSE)
}
}
\seealso{
Other install: 
\code{\link{check_install}()}
}
\concept{install}
