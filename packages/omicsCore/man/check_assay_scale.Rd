% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-omics_input.R
\name{check_assay_scale}
\alias{check_assay_scale}
\title{Check that the assay values look like the declared scale}
\usage{
check_assay_scale(x)
}
\arguments{
\item{x}{An \code{omics_input}.}
}
\value{
Invisibly, \code{TRUE} when the values match the declared scale,
\code{FALSE} when they do not.
}
\description{
\code{assay_type} is the only record of what scale \code{expr_mat} is on, and the
analysis backends act on it without re-deriving anything: limma runs
straight on \code{expr_mat}, while \code{log2(x + 1)} is applied only for
\code{"raw_count"}. Mislabelling therefore changes results silently -- feeding
vsn-normalised values to a \code{raw_intensity} path, or running limma on linear
intensities, produces plausible numbers that are wrong.
}
\details{
The check is a magnitude heuristic: log2 proteomics intensities top out
around 35, linear intensities run to millions. It warns rather than fails,
because a small or unusual matrix can legitimately land either side of
\link{MAX_PLAUSIBLE_LOG_SCALE_VALUE}.

Not called from \code{\link[=validate_omics_input]{validate_omics_input()}} on purpose: validation runs at
every analysis entry point, and this scans the matrix. \code{\link[=omics_input]{omics_input()}} runs
it once at construction, which is where a wrong label enters.
}
\seealso{
Other omics_input: 
\code{\link{SUPPORTED_ASSAY_TYPES}},
\code{\link{drop_meta_na}()},
\code{\link{is_omics_input}()},
\code{\link{normalize_omics}()},
\code{\link{omics_input}()},
\code{\link{select_complete_cases}()},
\code{\link{subset_omics}()},
\code{\link{subset_omics_features}()},
\code{\link{subset_omics_samples}()},
\code{\link{summarize_omics}()},
\code{\link{validate_omics_input}()}
}
\concept{omics_input}
