% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-integration.R
\name{run_integration}
\alias{run_integration}
\title{Run a cross-omics integration analysis}
\usage{
run_integration(
  project,
  method = c("correlation", "concordance", "active_pathways"),
  experiments = NULL,
  diff_bundles = NULL,
  by = "feature_symbol",
  ...
)
}
\arguments{
\item{project}{An \code{\link[=is_omics_project]{omics_project}} containing the
two experiments to integrate.}

\item{method}{One of \code{"correlation"}, \code{"concordance"}, \code{"active_pathways"}.}

\item{experiments}{Length-2 character vector of experiment tags. If
\code{NULL} and the project has exactly two layers, both are used.}

\item{diff_bundles}{Named list of \code{\link[=run_diff]{run_diff()}} bundles keyed by
experiment tag. Required for \code{"concordance"} and \code{"active_pathways"}.}

\item{by}{Feature key used to join layers (default \code{"feature_symbol"}).}

\item{...}{Method-specific arguments forwarded to the backend. See
the per-method sections below.}
}
\value{
An \code{\link[=is_analysis_bundle]{analysis_bundle}} with
\code{results$integration_df} (standardized schema) and, for
\code{"active_pathways"}, \code{results$integration_raw} carrying the raw
\code{ActivePathways} table.
}
\description{
Single entry point for dual-omics integration. Three methods are
supported:
}
\details{
\itemize{
\item \code{"correlation"} -- per-feature Pearson or Spearman correlation across
paired samples from two experiments. Useful for RNA / protein layers
that share donors.
\item \code{"concordance"} -- per-feature agreement between two differential
analyses, classified into the four sign quadrants and combined via
Fisher's method.
\item \code{"active_pathways"} -- pathway-level combined-p enrichment via the
\code{ActivePathways} package, fed by the p-values of two differential
analyses.
}

All methods return an \code{analysis_bundle} whose \code{results$integration_df}
follows the schema documented in \code{\link[=check_integration_result_schema]{check_integration_result_schema()}}.
}
\section{Correlation arguments}{

\itemize{
\item \code{cor_method} -- \code{"spearman"} (default) or \code{"pearson"}.
\item \code{p_adjust_method} -- defaults to \code{"BH"}.
\item \code{min_samples} -- minimum paired samples (default \code{4}).
\item \code{p_cutoff} -- significance cutoff (default \code{0.05}).
}
}

\section{Concordance arguments}{

\itemize{
\item \code{p_preference} -- \code{"adjusted"} (default) or \code{"raw"}.
\item \code{p_cutoff} -- significance cutoff (default \code{0.05}).
\item \code{p_adjust_method} -- defaults to \code{"BH"}.
}
}

\section{ActivePathways arguments}{

\itemize{
\item \code{database} -- MSigDB shorthand (default \code{"hallmark"}).
\item \code{organism} -- defaults to \code{"Hs"}.
\item \code{p_preference} -- \code{"raw"} (default) or \code{"adjusted"}. ActivePathways
prefers raw p-values since it applies its own multiple-testing
correction across pathways.
\item \code{significant} -- pathway-level cutoff (default \code{0.05}).
\item \code{geneset_filter} -- length-2 integer vector of min/max gene-set sizes
(default \code{c(5L, 1000L)}).
\item \code{merge_method} -- \code{"Brown"} (default) or another method accepted by
\code{ActivePathways::ActivePathways()}.
}
}

\examples{
\dontrun{
  # Correlation across paired RNA / protein samples
  cor_bundle <- run_integration(project, method = "correlation",
                                experiments = c("rna", "prot"))

  # Concordance from two diff bundles
  diff_rna <- run_diff(project$experiments$rna, ...)
  diff_prot <- run_diff(project$experiments$prot, ...)
  con_bundle <- run_integration(
    project, method = "concordance",
    experiments = c("rna", "prot"),
    diff_bundles = list(rna = diff_rna, prot = diff_prot)
  )
}
}
\seealso{
Other integration: 
\code{\link{plot_integration}()}
}
\concept{integration}
