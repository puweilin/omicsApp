% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-diff.R
\name{run_diff}
\alias{run_diff}
\title{Run a differential-expression analysis}
\usage{
run_diff(
  input,
  method = "auto",
  analysis_type = c("group", "continuous", "anova"),
  group_col = NULL,
  control_group = NULL,
  case_group = NULL,
  continuous_col = NULL,
  covariates = NULL,
  paired_col = NULL,
  selected_groups = NULL,
  ...
)
}
\arguments{
\item{input}{A validated \code{omics_input}.}

\item{method}{Backend name. \code{"auto"} (default) lets \code{omicsCore} pick one
based on \code{omics_type} and installed Suggests.}

\item{analysis_type}{One of \code{"group"}, \code{"continuous"}, or \code{"anova"}.}

\item{group_col}{Group column in sample metadata (group/anova).}

\item{control_group}{Control-group label (group only).}

\item{case_group}{Case-group label (group only).}

\item{continuous_col}{Continuous metadata column (continuous only).}

\item{covariates}{Optional character vector of covariate column names.}

\item{paired_col}{Optional pairing/block column.}

\item{selected_groups}{Optional subset of groups to retain (anova only).}

\item{...}{Extra arguments forwarded to the backend, e.g. \code{var_equal} for
t-test or \code{df = 3} for limma spline.}
}
\value{
An \code{\link[=is_analysis_bundle]{analysis_bundle}} with
\code{results$diff_result_df} (standardized schema), \code{results$diff_raw_df}
(backend-native), and \code{results$diff_object} (fitted model, may be
\code{NULL}).
}
\description{
Single entry point for proteomics and RNA-seq differential analysis.
Dispatches to the appropriate backend (DESeq2, edgeR, limma, t-test, or
linear model) and returns an \code{\link[=is_analysis_bundle]{analysis_bundle}}
wrapping the standardized result, the backend's raw table, and the fitted
model object.
}
\details{
\code{method = "auto"} picks limma for proteomics and DESeq2 for raw-count
RNA-seq. When the preferred Bioconductor backend is not installed, it
silently falls back to t-test / lm (with a message) so analyses still run
in restricted environments without \code{omicsCore::install_optional()} being
invoked first.

For paired designs supply \code{paired_col}; for ANOVA-style multi-group tests
set \code{analysis_type = "anova"} (currently limma-backed only). The
\code{continuous} analysis type requires \code{continuous_col} instead of
\code{group_col} + \code{control_group} + \code{case_group}.
}
\examples{
\dontrun{
  res <- run_diff(input,
                  analysis_type = "group",
                  group_col = "treatment",
                  control_group = "DMSO",
                  case_group = "Drug")
  head(res$results$diff_result_df)
}
}
\seealso{
Other diff: 
\code{\link{filter_diff_results}()},
\code{\link{make_ranked_features}()},
\code{\link{plot_feature_expression}()},
\code{\link{plot_heatmap}()},
\code{\link{plot_ma}()},
\code{\link{plot_pca}()},
\code{\link{plot_volcano}()},
\code{\link{run_diff_continuous}()}
}
\concept{diff}
