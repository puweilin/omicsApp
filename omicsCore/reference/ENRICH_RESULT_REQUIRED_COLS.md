# Standardized enrichment result columns

All enrichment backends (ORA, GSEA, GSVA) return a `data.frame` with
these columns.

## Usage

``` r
ENRICH_RESULT_REQUIRED_COLS
```

## Format

An object of class `character` of length 16.
