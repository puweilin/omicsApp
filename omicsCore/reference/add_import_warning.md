# Append a warning to an `ImportReport`

Append a warning to an `ImportReport`

## Usage

``` r
add_import_warning(report, msg)
```

## Arguments

- report:

  An `ImportReport`.

- msg:

  Character scalar warning message.

## Value

The updated `ImportReport`.
